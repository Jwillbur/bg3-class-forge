{
  "_readme": "Generated by make_fixtures.py. Do not hand-edit - regenerate.",
  "mod_name": "FixtureBlade",
  "parent": "Fighter",
  "fixtures": [
    {
      "id": "healthy",
      "breaks": "Nothing. This is unmodified `forge.py scaffold` output.",
      "symptom": "Subclass appears at character creation with real text.",
      "severity": "control",
      "expect": "every check must stay SILENT on this - a checker that flags the control is worse than no checker, because it trains you to ignore output",
      "still_parses_as_xml": true
    },
    {
      "id": "xml-malformed",
      "breaks": "Progressions.lsx is missing its closing </region>.",
      "symptom": "The mod fails to load, or loads with the progression table absent.",
      "severity": "crash",
      "expect": "check 15 - every .lsx parses as XML",
      "still_parses_as_xml": false
    },
    {
      "id": "using-cycle",
      "breaks": "Two passives in Passive.txt name each other with `using`.",
      "symptom": "Stats resolution never terminates on that entry.",
      "severity": "crash",
      "expect": "check 16 - `using` chains terminate",
      "still_parses_as_xml": true
    },
    {
      "id": "dangling-passive",
      "breaks": "Progressions grants FixtureBlade_NoSuchPassive, which no stats file defines.",
      "symptom": "CRASH AT LEVEL-UP - after the player has already committed to the choice, which is what makes this one worse than it looks.",
      "severity": "crash",
      "expect": "NOT YET IMPLEMENTED - every name in PassivesAdded resolves to a stats entry",
      "still_parses_as_xml": true
    },
    {
      "id": "table-uuid-mismatch",
      "breaks": "ClassDescriptions.ProgressionTableUUID does not equal Progressions.TableUUID.",
      "symptom": "The subclass silently does not appear. No error anywhere.",
      "severity": "silent",
      "expect": "NOT YET IMPLEMENTED - the two UUIDs agree. forge.py's template WARNS about this in a comment; a comment is not a check",
      "still_parses_as_xml": true
    },
    {
      "id": "unresolvable-parent",
      "breaks": "ParentGuid is a well-formed GUID that matches no class in the game data.",
      "symptom": "Crash or empty subclass list at character creation.",
      "severity": "crash",
      "expect": "NOT YET IMPLEMENTED - ParentGuid resolves against real game data. A shape check cannot see this: the value is a perfectly valid GUID",
      "still_parses_as_xml": true
    },
    {
      "id": "missing-loca-handle",
      "breaks": "The feature's DisplayName handle has no entry in the localisation file.",
      "symptom": "The raw handle string shows in the UI instead of a name.",
      "severity": "silent",
      "expect": "NOT YET IMPLEMENTED - every handle referenced has loca content",
      "still_parses_as_xml": true
    },
    {
      "id": "duplicate-uuid",
      "breaks": "The Progression node reuses the ClassDescription's UUID.",
      "symptom": "Undefined - one record shadows the other, and which one wins is not stable.",
      "severity": "silent",
      "expect": "NOT YET IMPLEMENTED - no UUID appears twice in the mod",
      "still_parses_as_xml": true
    },
    {
      "id": "dependency-absent",
      "breaks": "meta.lsx declares a dependency on a module that is not installed.",
      "symptom": "The mod is skipped, or the load order refuses to launch.",
      "severity": "crash",
      "expect": "NOT YET IMPLEMENTED - declared dependencies exist",
      "still_parses_as_xml": true
    }
  ]
}
